/**
 * Created by ernesto on 10/10/16.
 */
(function () {
    'use strict';

    angular
        .module('delfosApp')
        .factory('SharedActivity', SharedActivity);

    // SharedData.$inject = ['$resource'];

    function SharedActivity() {
        var Activity = {

        };

        return Activity;
    }

})();
